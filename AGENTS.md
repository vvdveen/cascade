 If a command fails due to insufficient permissions, you must elevate the command to the user for approval.

 If you change anything in the dependencies (i.e. under deps/), that are not in the current repo, always immediately
 update the scripts/patches/ files and commit them.



